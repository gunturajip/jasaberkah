---
import { Image } from "astro:assets";
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
import ByNumber from "./ByNumber.astro";

import hero1 from "../../assets/images/hero1.webp";
import hero2 from "../../assets/images/hero2.webp";
import hero3 from "../../assets/images/hero3.webp";
import CtaButton from "../shared/CtaButton.astro";
import ByResult from "./ByResult.astro";
import GradiantText from "../shared/GradiantText.astro";
import Portofolio from "../shared/Portofolio.astro";
import PSE from "../shared/PSE.astro";
import { lazy } from "astro/zod";

export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section class=`relative pt-32 lg:pt-36 ${className}`>
  <Container className={"flex flex-col lg:flex-row gap-10 lg:gap-12"}>
    <div
      class="relative flex flex-col items-center text-center lg:text-left lg:py-7 xl:py-8
        lg:items-start lg:max-w-none max-w-3xl mx-auto lg:mx-0 lg:flex-1 lg:w-1/2"
    >
      <h1
        class="text-3xl/tight sm:text-4xl/tight md:text-5xl/tight xl:text-6xl/tight
             font-bold text-heading-1"
      >
        Permudah Pengurusan
        <GradiantText>Izin Usaha</GradiantText>
        Anda Dengan Berkah Jasa
      </h1>
      <Paragraph className="mt-8 ">
        Mulai izin pendirian <GradiantText><strong>CV, PT, berbagai izin usaha lainnya</strong></GradiantText>, dari wilayah <GradiantText><strong>Provinsi Jawa Timur</strong></GradiantText> hingga <GradiantText><strong>seluruh Indonesia</strong></GradiantText>.
      </Paragraph>

      <div class="mt-8 w-full flex flex-col">
        <div class="md:gap-8 gap-4 flex flex-col md:flex-row">
          <div class="lg:mx-auto mb-8">
            <Portofolio />
          </div>
          <div class="lg:mx-auto mb-8">
            <CtaButton />
          </div>
          <div class="lg:mx-auto mb-8">
            <PSE />
          </div>
        </div>
        <div class="mx-auto">
          <ByResult />
        </div>
      </div>
    </div>

    <div class="relative flex-1 lg:w-1/2 lg:h-auto lg:max-w-none lg:mx-0 mx-auto max-w-3xl">
      <div class="relative w-full h-96 sm:h-full lg:aspect-square aspect-[3/4] overflow-hidden rounded-3xl">
        <Image
          loading="lazy"
          src={hero1}
          alt="Berkah Jasa Surabaya"
          width="2350"
          height="2359"
          class="carousel-img absolute inset-0 w-full h-full object-cover opacity-100 z-20 transition-opacity duration-1000"
        />
        <Image
          loading="lazy"
          src={hero2}
          alt="Berkah Jasa Sidoarjo"
          width="2350"
          height="2359"
          class="carousel-img absolute inset-0 w-full h-full object-cover opacity-0 z-10 transition-opacity duration-1000"
        />
        <Image
          loading="lazy"
          src={hero3}
          alt="Berkah Jasa Gresik"
          width="2350"
          height="2359"
          class="carousel-img absolute inset-0 w-full h-full object-cover opacity-0 z-10 transition-opacity duration-1000"
        />
      </div>
    </div>
  </Container>

  <ByNumber />
  <script type="module">
    const images = document.querySelectorAll('.carousel-img');
    let current = 0;

    setInterval(() => {
      images.forEach((img, i) => {
        img.style.opacity = i === current ? '1' : '0';
        img.style.zIndex = i === current ? '20' : '10';
      });

      current = (current + 1) % images.length;
    }, 4000); // Change image every 4 seconds
  </script>
</section>
